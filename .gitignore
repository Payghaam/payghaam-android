# Machine-specific Android SDK path — required so this module resolves as
# an included build (see sdks/android/local.properties and the comment in
# sdks/android/settings.gradle.kts), but must not be committed since the
# path differs per developer machine.
local.properties
